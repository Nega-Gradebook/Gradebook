<p id="notice"><%= notice %></p>

<h1>Listing Teachers</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @teachers.each do |teacher| %>
      <tr>
        <td><%= teacher.name %></td>
        <td><%= teacher.email %></td>
        <td><%= link_to 'Show', teacher %></td>
        <td><%= link_to 'Edit', edit_teacher_path(teacher) %></td>
        <td><%= link_to 'Destroy', teacher, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<div><%= link_to 'Add Teacher', new_teacher_path %></div>
<div><%= link_to 'Add Parent', new_parent_path %></div>
<div><%= link_to 'Add Student', new_student_path %></div>
<div><%= link_to 'Add Grade', new_grade_path %></div>
<br />

<div><%= link_to 'Teacher Index', teachers_path %></div>
<div><%= link_to 'Parent Index', parents_path %></div>
<div><%= link_to 'Student Index', students_path %></div>
<div><%= link_to 'Grade Index', grades_path %></div>
